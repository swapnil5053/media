PORT=8000
APP_URL=http://localhost:8000

# Used to sign session and playback cookies. Generate with: openssl rand -hex 32
SESSION_SECRET=change-me-before-deploying

# Where SQLite and uploaded media live. Mount this as a volume in production.
DATA_DIR=./data
