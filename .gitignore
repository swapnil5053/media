node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
metadata.json
sample.mp4
uploads/
.vscode/

# DATA_DIR — the SQLite database and every uploaded video land here.
data/

# Full-resolution originals for the hero frames. The 640x360 crops that ship
# live in public/stills; these are 15 MB of phone photos and stay local.
Background_images/

# Working notes for the rebuild, and the design export they came from.
REBUILD_PROMPT.md
DESIGN_*.md
AdaptFlow landing page/
AdaptFlow landing page*
